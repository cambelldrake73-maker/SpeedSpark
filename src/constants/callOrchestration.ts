/** Seconds to wait for partner to join before no-show cancel (Phase 5). */
export const CALL_JOIN_GRACE_SECONDS = 45;

/** Seconds to wait for partner reconnect after mid-call disconnect. */
export const CALL_RECONNECT_GRACE_SECONDS = 20;
