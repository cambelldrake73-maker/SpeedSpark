import type { Match, Message } from '../types';

/** Demo matches and messages are created at runtime after mutual matches. */
export const MOCK_MATCHES: Match[] = [];
export const MOCK_MESSAGES: Message[] = [];
